{
  "name": "pr-slop-gate",
  "version": "0.1.0",
  "description": "AI-aware pull-request triage GitHub Action for OSS maintainers — scores PRs for AI slop with diff heuristics + an LLM grader, then labels/comments/closes with trusted-contributor exemptions.",
  "type": "module",
  "license": "MIT",
  "private": false,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "github-action",
    "pull-request",
    "ai-slop",
    "spam",
    "triage",
    "oss-maintainers"
  ],
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
